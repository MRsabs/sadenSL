{
  "dependencies": {
    "electron-build-env": "^0.2.0",
    "neon-cli": "^0.4.0"
  },
  "scripts": {
    "predev": "npm i",
    "prebuild": "npm i",
    "dev": "electron-build-env neon build",
    "build": "electron-build-env neon build --release"
  }
}

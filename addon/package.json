{
  "dependencies": {
    "electron-build-env": "^0.2.0",
    "neon-cli": "^0.4.0"
  },
  "scripts": {
    "predevelopment": "npm i",
    "preproduction": "npm i",
    "development": "electron-build-env neon build",
    "production": "electron-build-env neon build --release"
  }
}
